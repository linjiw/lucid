# LUCID autoresearch ledger

Objective: continue the handoff queue in order: measure idle-GPU throughput, validate live latency, resolve warmup/no-op hygiene, and run the preregistered capsule-resume check before claim-bearing screening.

Metrics: steady-state SONIC transitions per second; fixed-lambda reward/length movement against the frozen noise floors; zero-tolerance warmup/no-op parity; and 20-vs-10+10 last-4 resume deltas. Scientific outcomes are retained regardless of sign; implementation variants are kept only when their tests and receipt contracts pass.

Constraints: use the pinned `lucid_env.sh`; start training comparisons from the settled checkpoint; never modify upstream SONIC implementation files; write an experiment receipt with `verified` and `not_yet_verified`; do not implement the utility estimator or residual allocator before Gate B. The failed resume-equivalence gate caps screening as engineering evidence until L0 is repaired or the estimand is explicitly redesigned.
